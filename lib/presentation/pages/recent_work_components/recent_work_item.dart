import 'package:flutter/material.dart';

class RecentWorkItem extends StatelessWidget {
  const RecentWorkItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
